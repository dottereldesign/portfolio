# Project instructions

This is Jamie Wilson’s portfolio repository.

After every change to this project:

1. Check the work and run any relevant verification.
2. Commit the change with a clear, concise message.
3. Push the commit to the configured GitHub remote.

Never force-push or overwrite unrelated user changes. If pushing is blocked, explain the reason clearly instead of silently skipping it.
